.. _chapter-02-02-instruction-set:

==========================
IR 指令集
==========================

.. TODO: 本节内容

    - 内存指令：alloca、load、store、getelementptr
    - 算术指令：add、sub、mul、udiv/sdiv、urem/srem、fadd 等
    - 比较指令：icmp、fcmp
    - 控制流指令：br、switch、ret、unreachable、phi、call、indirectbr
    - 聚合操作指令：insertvalue、extractvalue、insertelement、extractelement、shufflevector
    - 类型转换指令：trunc、zext、sext、fptoui、uitofp、bitcast、addrspacecast 等
    - 其他指令：select、va_arg、landingpad、resume、cleanupret 等
    - GEP 指令的深入理解
