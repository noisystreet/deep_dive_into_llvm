.. _chapter-01-03-hello-world:

==========================
Hello World
==========================

.. TODO: 本节内容

    - 安装 LLVM/Clang 开发环境
    - 从 C 源码到可执行文件的完整流程：clang 编译过程
    - 查看中间产物：-emit-llvm、-S、-c 等选项
    - 第一个 LLVM Pass：命令行工具使用初体验（opt）
    - 第一个 JIT 示例：使用 lli 直接运行 IR
    - 构建一个最小的 LLVM 项目（CMake + LLVM libraries）
